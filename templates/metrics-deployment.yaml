{{- if .Values.metric_svc.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.metric_svc.name }}
  labels:
    app: {{ .Values.metric_svc.name }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.metric_svc.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.metric_svc.name }}
    spec:
      containers:
        - name: {{ .Values.metric_svc.name }}
          image: "{{ .Values.metric_svc.image.repository }}:{{ .Values.metric_svc.image.tag }}"
          ports:
            - containerPort: {{ .Values.metric_svc.containerPort }}
{{- end }}
